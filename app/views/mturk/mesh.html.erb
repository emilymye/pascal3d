<div class="error hide">
  Error: Your browser does not support WebGL - you cannot do this task.<br>
  You can enable WebGL in your browser by following the instructions below. <br>
  
  <p><span style="font-weight:bold">Internet Explorer</span></p>
  <p><span style="font-weight:bold">Chrome</span></p>
  <p><span style="font-weight:bold">Safari</span><br> 1) Open Safari-Preferences...<br> 2) Go to "Advanced" tab.<br> 3) At the bottom, check "Show Develop menu in menu bar" in the checkbox.<br> 4) In the menubar, you can now find "Develop" menu. Select "Enable WebGL".<br>
  </p>
  <p><span style="font-weight:bold">Firefox</span></p>
</div>


<div class = "container">
  <% if @annotation.nil? %>
    <div class="error">Error- this HIT is invalid. Please disregard.</div>
  <% else %> 
    <% cat_pretty = @annotation.category_name.humanize.downcase %>
    <form id="mesh_form" action="<%= @formurl %>"  method="post">
      <%= hidden_field_tag 'assignmentId', @assignmentId%>
      <%= hidden_field_tag 'task', "mesh" %>
      <%= hidden_field_tag 'annotation_id', @annotation.id %>

    <div class= "instructions_mesh">
      <!--<h1>Match a 3D model to the object.</h1>-->
      <h2>Instruction</h2>
      <p>Choose the 3D model that most closely matches the indicated <b><%= cat_pretty %></b> within the red box in the given image. We show some examples.</p>
     
      <b>Examples:</b><br>
      <div class="table"><div class="table-row">
      <div class="table-cell">
        category "chair"<br>
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/chair.png" height="100" width="140" align="middle">
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/chair_mesh.png" height="100" width="140" align="middle">
      </div>
      <div class="table-cell">
        category "airplane"<br>
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/aeroplane.png" height="100" width="140" align="middle">
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/aeroplane_mesh.png" height="100" width="140" align="middle">
      </div>
      <div class="table-cell">
        category "motorbike"<br>
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/motorbike.png" height="100" width="140" align="middle">
        <img src="http://cvgl.stanford.edu/projects/PASCAL3D_AMT/mesh_instruction/motorbike_mesh.png" height="100" width="140" align="middle">
      </div>
      </div></div>
      <br>You <span style="color:red;font-weight:bold">MUST</span> make one choice. If you submit without choosing anything, we will <span style="font-weight:bold;color:red">REJECT</span> the Hit.<br>

    </div>

    <!--target image with bounding box annotation-->
    <div class= "images_mesh">
      <br><h1>Match a 3D model to the <u><%= cat_pretty %></u>.</h1>
      <%= render :partial => "annotated_image", :locals => { :img_file => @annotation.image_file, :annotation => @annotation } %>
    </div>

    <!--preview or submit button-->
    <% if @preview %>You will se a submit button here once you have accepted the task.<br>
    <% else %><%= submit_tag 'Submit', :class => "hide btn btn-green" %><br><br>
    <% end %>

    <div class= "table">
      <% @meshes.each_with_index do |m, index| %>
        <% if index.modulo(4).zero? %> <!--create a new row after every 4 cells-->

          <div class= "table-row">
            <% for i in 0..3%>
              <% if (index+i+1) > @meshes.length then %><% break %><% end %>
              <div class= "table-cell"><div class="mesh-option">
		<% if (index+i+1) == @meshes.length then %>
		  <%= render :partial => "gl_viewer", :locals => {:small => true, :mesh => @meshes[index+i], :is_mesh => false} %>
		<% else %>
                  <%= render :partial => "gl_viewer", :locals => { :small => true, :mesh => @meshes[index+i], :is_mesh => true } %>
		<% end %>
                <br/><input type="radio" name="mesh_file" required value="<%=@meshes[index+i]%>" />
              </div></div>
            <% end %>                                         
          </div>

        <% end %>
      <% end %>
    
  </form>

<% end %>
</div>

<script>

$(function(){
  if ( !Detector.webgl ){
    $(".error").show();
    $(".container").remove();
    return false;
  }

  var toLoad = $(".mesh-option").length;
  console.log("Meshes to load: " + toLoad);
  function onloaded(canvasid){
    toLoad--;
    console.log("Meshes to load: " + toLoad);
    $("#" + canvasid).next().hide();
    if (toLoad <= 0 && <%= @preview %>){
      $("input[type='submit']").show();
    }
  }
  

  $(".mesh-option").map(function(){
    var mesh = $(this).find("input").val();
    var canvasid = $(this).find("canvas").attr("id");
    console.log(canvasid);
    var o = new ObjectViewer( mesh, canvasid, { 
      orbit_speed: 0.01, 
      onLoaded: function() { 
        onloaded(canvasid) 
      }
    });
  });
})

</script>
